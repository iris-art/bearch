<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bearch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.apps.photos.permission.GOOGLE_PHOTOS"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="false"
        android:largeHeap="true">
        <activity android:name=".memberActivity"></activity>
        <activity android:name=".requestsActivity" />
        <activity android:name=".musicanDetail" />
        <activity android:name=".bandDetail" />
        <activity android:name=".saveUser" />
        <activity android:name=".logoutActivity" />
        <activity android:name=".bandResultActivity" />
        <activity android:name=".musicanResultActivity" />
        <activity android:name=".FilterMusicanActivity" />
        <activity android:name=".FilterBandActivity" />
        <activity android:name=".bandActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".CreateBandActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>